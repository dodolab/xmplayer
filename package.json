{
  "scripts": {
    "start": "node deploy.js && parcel view/*.html --out-dir dist",
    "start_no_map": "node deploy.js && parcel view/*.html --out-dir dist --no-source-maps",
    "deploy": "node deploy.js && parcel build view/*.html --out-dir dist --no-source-maps"
  },
  "devDependencies": {
    "parcel-bundler": "1.11.0",
    "parcel-plugin-json-url-loader": "0.1.2",
    "typescript": "^3.1.1",
    "tslint": "5.12.1"
  }
}
