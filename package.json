{
  "scripts": {
    "start": "node scripts/generate_files.js && parcel view/*.html --dist-dir  dist",
    "start_no_map": "node scripts/generate_files.js && parcel view/*.html --dist-dir dist --no-source-maps",
    "build": "node scripts/generate_files.js && parcel build view/*.html --dist-dir dist --no-source-maps"
  },
  "devDependencies": {
	"@types/jest": "^27.0.2",
	"@typescript-eslint/eslint-plugin": "^5.1.0",
	"@typescript-eslint/parser": "^5.1.0",
	"eslint": "^7.32.0",
	"eslint-plugin-import": "^2.22.1",
	"rimraf": "^3.0.2",
	"tslib": "^2.5.0",
	"typescript": "^4.4",
	"parcel": "^2.8.3"
  }
}
